<h1>Posts#index</h1>

<ul>  
<% @posts.each do |post| %> 
  <li><%= post.title %> <%= link_to 'Edit',edit_post_path(post.id)%> | <%= link_to 'Show',show_post_path(post.id)%> |  <%= link_to 'Delete',delete_post_path(post.id), method: :delete %> </li>
<% end %>
</ul>


<p><%= link_to 'New Post', new_post_path %></p>
